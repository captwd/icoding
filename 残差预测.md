# 残差预测

写这篇文章的起因是在微信公众号里偶然看见
> 当LSTM预测不准时怎么办”，
“让随机森林专门去拟合残差”，

这个***去拟合残差***突然就让我有了点感悟。
首先是LSTM适合从序列的连续性里找规律，而随即森林更擅长的是去特征的非线性组合里挖细节，最终的输出是两个部分的混合预测
从这个角度看，用随机森林去补强模型性能是一个很不错的选择。

## 残差预测与ResNet
但是，**残差**这个词让我想到了ResNet，那残差模块和这个残差有什么联系呢
ResNet的实现很简单，表面上看就是

$$
y = F(x) + x
$$

我曾经的理解比较简单，只是单纯地从偏导的角度去理解，x偏导之后会多出一个1，能够有效避免梯度消失，这一点在长文本任务中极其重要，因此即便是Transformer架构在处理长文本能力上比RNN强很多，也依然用上了残差连接，来防止网络过深的情况下带来的梯度消失

但其实它背后也含有一个***残差学习***的思想
原本我们是想让网络模型去学习一个从$x->y$的映射，然而这里通过加一个x,实现了模型只需要去学习$F(x)=y-x$中的这个F(x),即实际值与预测值的差值。

最理想的情况就是F(x)等于0，即保持原样输出，只需把最后的几层模型权重弄到0附近，而如果不使用残差模块，那么模型就还会费劲心思地去学一个镜面映射，让$y=x$

这里举两个简单的例子吧
### 1.以考试成绩为例
```
x = 90（你原本的底子）
H(x) = 95（最终理想结果）
F(x)=H(x)−x = 5（需要额外学习的那个“差值”）
```
在这个例子里，模型只需要去学习怎么弥补这个差值，等于去看错题复习，而不用残差模块，等于去看整张卷子巩固，效率上来说肯定是看错题复习高一点
### 2.以改文章为例
原本模型是需要模仿现有的文章风格，把整篇文章重新写一遍，并且要写成符合要求的最终版本，而残差学习就相当于是先保留原文，然后在原文上标注出需要更改的地方。

在视觉领域可能会更直观一点，输入到某一层的特征，不会和输出完全无关，很多信息本来就该保留，真正需要学习的只是一些修正，比如原本已经学会了特征，现在只需要强调某些边缘特征。（感觉有点像Diffusion？）
## shortcut / Skip Connection
这两个词翻译过来可以是残差连接，但我感觉叫跨层连接更加方便理解。深度学习过程中，往往会因为层数过深，导致上层的信息很难传到下层，而且也容易逐渐“忘记”上层已经学到的特征。通过添加一个残差模块，就能够实现跨层传递信息和梯度，始终为模型提供一个更稳定的通路
***普通神经网络***
```
输入 → 层1 → 层2 → 层3 → 层4 → 输出
        ↓    ↓    ↓    ↓
        逐层映射     无旁路
```
***残差神经网络***
```
          ┌─────────────直连旁路(恒等映射)─────────────┐
          │                                           │
输入    → 层1   → 层2   → 层3   → 逐元素相加   ⊕   → 输出
```
## DenseNet
DenseNet处理历史信息的能力比ResNet更强，如果说ResNet是做一个加法，那DenseNet就是做一个拼接，把历史信息在***通道维***上拼接起来传递到下一层，使得网络在做非线性变换时，历史信息被完全无损地、显式地保存了下来。

$$ 
Xl=Hl([X 0,X 1,…,X l−1])
$$

在 DenseNet 的前向传播中，信息的流动可以概括为构建一种 “***全局特征状态***”

因为不需要每层都去“重新学习”或“复制”前传过来的重复特征，映射函数被极大地减负了。它的任务被严格限定为只寻找当前网络状态中依然缺失的“新知识”，即新增的那 k 个通道（每次在通道层进行拼接），从而避免了网络深层的特征冗余。

DenseNet 的蛛网式直接梯度通道（Direct Gradient Connections）：
由于第l层直接参与了后续所有层的组成，在求偏导时，整个网络的每一层都在接收来自目标函数的直接误差信号。这迫使早期的浅层网络就能学习到极其具有判别性的特征

当然这种方式的缺点也很明显，DenseNet时进行张量拼接而不是直接相加，GPU 在处理张量拼接时，通常需要开辟一块新的连续物理显存，并把 A 和 B的数据重新深拷贝进去，这对GPU及其不友好，而且由于后续通道的不断拼接，对显存的占用增长极快，极其频繁的内存读写，也受限于I/O瓶颈限制
